spring:
  datasource:
    url: jdbc:tc:postgresql:14:///test-db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: create

  liquibase:
    enabled: false

eureka:
  client:
    enabled: false
